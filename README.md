## mi-po

for nodes deployment:
set env vars in `.env`

```
DATABASE_URL
TWILIO_ACCOUNT_SID
TWILIO_AUTH_TOKEN
TWILIO_VERIFY_SERVICE_SID  (Create a Verify service)
JWT_SECRET
JWT_EXPIRATION_TIME (in miliseconds)
```

set url in client.ts to NEXT_PUBLIC_BASE_URL
